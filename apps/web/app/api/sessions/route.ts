import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({ message: 'Sessions API — not yet implemented' });
}

export async function POST() {
  return NextResponse.json({ message: 'Sessions API — not yet implemented' });
}
